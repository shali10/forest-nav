{
  "name": "forest-nav",
  "version": "1.1.1",
  "private": true,
  "description": "高饱和旅行海报风个人导航页，支持 Cloudflare 一键部署。",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check worker.js && node --check functions/api/posts.js && node --check functions/api/weather.js"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
